

## Перенос пунктов меню и удаление панели управления

### Изменения

**1. `src/components/AppSidebar.tsx`**
- Убрать из `mainNav`: «Панель управления», «Теги», «Таблица описаний», «Архив»
- В `mainNav` остаются: «Платные программы», «Конструктор писем»
- Добавить в `adminNav`: «Теги», «Таблица описаний», «Архив»
- Убрать неиспользуемые иконки (`LayoutDashboard`)

**2. `src/App.tsx`**
- Удалить импорт `Dashboard` и страницу `Index`
- Изменить маршрут `/` → редирект на `/programs`
- Маршруты `/tags`, `/descriptions`, `/archive` сделать `adminOnly`

**3. Файлы `src/pages/Dashboard.tsx` и `src/pages/Index.tsx`** — можно удалить (опционально, не сломает ничего).


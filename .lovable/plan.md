

## Вынести `{{lead_magnet}}` и `{{USER_ANSWERS}}` в отдельные карточки

Убрать эти две переменные из категории «Оффер» и создать для них отдельные карточки:

**`src/pages/PromptVariables.tsx`**:
- Убрать `{{lead_magnet}}` и `{{USER_ANSWERS}}` из массива переменных категории «Оффер»
- Добавить две новые категории в массив `categories`:
  - `"Лид-магниты"` с переменной `{{lead_magnet}}`
  - `"Диагностики"` с переменной `{{USER_ANSWERS}}`




## Задача

Заменить кнопку «Выбрать» на кнопку просмотра карточки кейса (как в классификаторе). Выбор кейса — кликом по строке.

## Изменения

### `src/components/email-builder/CasePickerDialog.tsx`

1. **Добавить состояние** `previewCase` для показа карточки классификации (аналог `jsonDialog` из CaseManagement).

2. **Последний столбец** (строки 250-259): заменить кнопку «Выбрать» / бейдж «Выбран» на иконку-кнопку `<Eye>` для просмотра. Для выбранной строки — оставить подсветку `bg-accent` и бейдж `✓` рядом с иконкой.

3. **Клик по строке** (строка 211): уже вызывает `handleSelect` — оставить как есть, это и есть выбор кейса.

4. **Кнопка Eye** (с `e.stopPropagation()`) — открывает вложенный `Dialog` с карточкой классификации: цитата, до/после, ключевые инсайты, рекомендовано для, полный JSON (как в CaseManagement, строки 628-673).

5. **Вложенный диалог** рендерится внутри компонента — карточка с деталями кейса и кнопкой «Выбрать этот кейс» внутри превью.

Итого: 1 файл, ~50 строк добавлений/изменений.


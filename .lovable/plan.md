
Проверил текущее состояние по диагностике `04333214-f2d8-4a7d-bef5-bd235fba8609`:

- В базе уже стоит `status = error`
- `generation_progress.error = "Остановлено пользователем"`
- Активных диагностик в статусах `generating / quiz_generated / generating_images` сейчас нет

Итог: генерация уже остановлена вручную на уровне backend, дополнительно “убивать” нечего.

План, чтобы это больше не выглядело как “еще крутится” в UI:

1) Синхронизировать шаги прогресса после стопа  
- В `DiagnosticDetail.tsx` пересчитывать `steps` не только для активных статусов, но и для `error`, чтобы не оставалось старое `1 из 12` со спиннером.

2) Явно отображать “остановлено” в шаге изображений  
- При `handleStop` сохранять в `generation_progress` также текущие `total_images/completed_images` (если есть), и переводить шаг “Создание изображений” в `error` с подписью вроде `Остановлено (1 из 12)`.

3) Убрать ложный сигнал об активной генерации  
- Для статуса `error` с текстом “Остановлено пользователем” показывать информационное сообщение вместо общего “Ошибка при генерации”.

Технически причина: локальный `steps` остается от последнего активного состояния, потому что обновление шагов сейчас запускается только когда статус активный.



## Добавить переменные диагностики в справочник + исправить generate-card-prompt

### Проблема
1. Из справочника переменных убрали все переменные диагностик, но некоторые по-прежнему используются (просто теперь маппятся на универсальные offer-переменные).
2. В `generate-card-prompt/index.ts` (строки 114-116) остались старые имена `test_name`, `test_description`, `audience_tags` — нужно обновить на `offer_title`, `offer_description` и убрать `audience_tags`.

### Изменения

**1. `src/pages/PromptVariables.tsx`** — добавить категорию «Диагностика (тест)» с переменными, специфичными для пайплайна `test_generation`:
- `{{offer_title}}` — уже есть в «Оффер», но стоит пояснить что для диагностик это название теста
- `{{USER_ANSWERS}}` — JSON с ответами пользователя (используется в промптах генерации результатов, если будет добавлено позже)

Либо проще: не добавлять отдельную категорию, а просто добавить `{{USER_ANSWERS}}` в категорию «Оффер» как переменную, доступную для диагностик.

**2. `supabase/functions/generate-card-prompt/index.ts`** — обновить templateVars (строки 110-118):
- `test_name` → `offer_title`
- `test_description` → `offer_description`  
- Убрать `audience_tags`


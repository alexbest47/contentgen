

## Привести список офферов к табличному виду (как у диагностик)

### Файл: `src/pages/OfferTypeManagement.tsx`

Заменить текущий список (div с `divide-y`) на компонент `Table` внутри `Card`, аналогично `Diagnostics.tsx`:

- Добавить импорт `Table, TableBody, TableCell, TableHead, TableHeader, TableRow`
- Добавить импорт `Loader2`
- Колонки таблицы: **Программа** | **Название** | **Действия** (text-right)
- Каждая строка: программа из `paid_programs`, название оффера, кнопки (Открыть, Редактировать, В архив)
- Заменить спиннер загрузки на `Loader2` с анимацией
- Убрать `ChevronRight` и inline-теги из строк — оставить чистую таблицу

Структура строки действий (как у диагностик):
- Кнопка «Открыть» (ghost, `Eye` icon) → навигация к деталям
- Кнопка редактирования (ghost, `Pencil` icon) → открывает edit dialog
- Кнопка архивирования (ghost, `Trash2` icon, destructive) → открывает confirm dialog

Один файл, только изменение разметки списка — вся логика (CRUD, диалоги) остаётся без изменений.


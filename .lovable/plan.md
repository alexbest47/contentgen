

## Задача

Удалить неиспользуемый промпт «Блок подборки офферов» из таблицы `prompts`.

## Изменения

### SQL-миграция

```sql
DELETE FROM public.prompts WHERE name = 'Блок подборки офферов';
```

Код не затрагивается — промпт не используется ни в одной Edge-функции или компоненте.


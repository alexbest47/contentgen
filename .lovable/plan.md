

## Добавить колонку «Шаблон» в список писем

### Изменения

**`src/pages/EmailBuilderList.tsx`**

1. Изменить запрос: вместо `select("*")` → `select("*, email_templates(name)")` для подтягивания имени шаблона через `template_id`
2. Добавить колонку «Шаблон» в таблицу (между «Тема контента» и «Обновлено»)
3. Отображать `letter.email_templates?.name || "—"`

### Итого
- 1 файл, ~5 строк добавлено/изменено


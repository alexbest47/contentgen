

## Разделить «Подготовка контента» на два блока в сайдбаре

### Файл: `src/components/AppSidebar.tsx`

1. Переименовать массив `contentPrepNav` — оставить в нём только: Управление кейсами, Дерево тем, Работа с возражениями.
2. Создать новый массив `offerPrepNav` с одним пунктом: Диагностики (`/diagnostics`, иконка `Stethoscope`).
3. В JSX добавить новую `SidebarGroup` с лейблом «Подготовка офферов» между «Подготовка контента» и «Администрирование», рендерящую `offerPrepNav`.

Итого: правка одного файла, ~10 строк.


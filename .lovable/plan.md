

## Добавить кнопку «Перегенерировать» на страницу списка диагностик

Сейчас кнопка перегенерации есть только на странице деталей (`DiagnosticDetail`). Нужно добавить её прямо в таблицу на `/diagnostics`.

### Изменения в `src/pages/Diagnostics.tsx`

- Добавить кнопку «Перегенерировать» (иконка `RefreshCw`) в колонку действий для диагностик со статусом `ready` или `error`
- По клику: обновить статус на `generating`, вызвать `run-diagnostic-pipeline` (fire-and-forget), показать toast и перенаправить на страницу деталей (где уже работает polling прогресса)
- Для этого нужно также подгружать `prompt_id` в запросе диагностик (добавить в select)


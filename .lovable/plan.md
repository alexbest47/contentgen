

## Добавить подтверждение перед экспортом HTML

### Что нужно сделать
При нажатии «Экспортировать HTML» вместо немедленного экспорта показать `AlertDialog` с вопросом «Вы уверены, что добавили в письма все необходимые ссылки?» и двумя кнопками: «Да» (выполняет экспорт) и «Вернуться к письму» (закрывает диалог).

### Изменения

**`src/components/email-builder/EmailBuilderHeader.tsx`**:
1. Добавить state `confirmExportOpen` (boolean).
2. Кнопку «Экспортировать HTML» привязать к `setConfirmExportOpen(true)` вместо прямого вызова `onExportHtml`.
3. Добавить `AlertDialog` с:
   - Заголовок: «Вы уверены, что добавили в письма все необходимые ссылки?»
   - Кнопка «Да» → вызывает `onExportHtml()` и закрывает диалог
   - Кнопка «Вернуться к письму» → просто закрывает диалог
4. Импортировать компоненты `AlertDialog` из `@/components/ui/alert-dialog`.

Один файл, минимальные изменения.


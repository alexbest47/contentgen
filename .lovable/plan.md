

## Переименовать «Переход к курсу» → «Переход к офферу»

Одно изменение в `src/pages/ProjectDetail.tsx`, строка 180 — заменить текст лейбла.

Колонку в БД (`transition_to_course`) не трогаем — это внутреннее имя, пользователь его не видит.


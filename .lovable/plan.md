
Проблема, судя по коду и runtime-данным, не в сохранении результата в backend: задача в очереди уже помечается как completed после того, как письмо записано в базу. Сбой происходит на фронте — при переходе из очереди иногда открывается пустое/устаревшее состояние письма, а при повторном заходе уже подхватывается свежий кэш.

Что сделаю:

1. Исправлю открытие письма в `src/pages/EmailBuilder.tsx`
- Добавлю явное состояние первичной загрузки для существующего письма.
- Пока запрос письма/блоков не подтвердился, не буду рендерить пустой конструктор со state по умолчанию.
- Сообщение «Письмо не найдено или было удалено» буду показывать только после завершённого запроса, а не во время промежуточной загрузки.

2. Сделаю обновление письма после завершения генерации
- В `generateLetter()` добавлю тот же polling `task_queue`, который уже используется для генерации placeholder-изображений.
- После статуса `completed` буду инвалидировать `email_letter` и `email_letter_blocks`, чтобы страница подхватывала свежий HTML сразу, без повторного входа.

3. Уберу эффект “первый клик — пусто, второй — уже есть” из очереди задач
- В `src/pages/TaskQueue.tsx` перед переходом по ссылке на `/email-builder/:id` буду очищать/инвалидировать query cache для этого письма.
- Это заставит экран открываться с актуальной загрузкой, а не с потенциально старым кэшированным пустым состоянием.

4. Сохраню текущую обработку удалённых писем
- Если письмо реально удалено, останется понятная заглушка.
- Если письмо существует, пользователь увидит либо loader, либо уже готовый контент — без ложного “пустого письма”.

Технически:
- Файлы: `src/pages/EmailBuilder.tsx`, `src/pages/TaskQueue.tsx`
- Изменений в БД и backend-функциях не требуется
- Основа фикса: убрать преждевременный рендер пустого UI и синхронизировать кэш React Query с завершением задачи

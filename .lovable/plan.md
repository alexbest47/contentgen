

## Разбить «Администрирование» и добавить «Настройка email»

### Файл: `src/components/AppSidebar.tsx`

1. **Новый массив `emailSettingsNav`**:
   - «Шаблоны писем» → `/email-templates`, иконка `FileText`
   - «Настройка хедера и футера» → `/email-settings`, иконка `Mail`

2. **Обновить `adminNav`** — убрать из него «Шаблоны писем» и «Настройки Email», оставить:
   - «Управление промптами»
   - «Переменные промптов»
   - «Пользователи»

3. **В JSX** добавить новую `SidebarGroup` с лейблом «Настройка email» между «Администрирование» и футером, рендерящую `emailSettingsNav`.

Итого: правка одного файла, ~15 строк.


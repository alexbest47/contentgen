
Проблема локализована в `src/components/email-builder/CreateLetterWizard.tsx`.

Что сломано:
- После перестановки шагов кнопка «Далее» на шаге 2 («О чём это письмо?») всё ещё использует старую логику.
- Сейчас `handleNext1()` в обычном режиме делает `setStep(2)`, хотя после выбора темы должен переводить wizard на шаг 3.
- Поэтому тема выбирается, кнопка может быть активной, но перехода дальше не происходит.

План исправления:
1. Обновить `handleNext1()`:
   - оставить `themeOnlyMode` без изменений: применить тему и закрыть окно;
   - в обычном режиме менять шаг с `2` на `3`, а не на `2`.

2. Быстро проверить связанную навигацию после перестановки шагов:
   - шаг 1 → шаг 2 по `canNext3` (шаблон);
   - шаг 2 → шаг 3 по `canNext1` (тема);
   - шаг 3 → шаг 4 по `canNext2` (аудитория);
   - `Назад` должен продолжать работать последовательно.

3. Прогнать точечную регрессию в этом же компоненте:
   - обычный сценарий создания письма;
   - `themeOnlyMode`, который должен по-прежнему открываться сразу на шаге 2 и завершаться кнопкой «Применить», без перехода на шаг 3.

Технически:
- Файл: `src/components/email-builder/CreateLetterWizard.tsx`
- Ожидаемый объём: 1 маленькая правка в обработчике + быстрая сверка условий навигации.
- Изменений в базе данных и backend не требуется.

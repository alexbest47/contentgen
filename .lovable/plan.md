

## Свернуть все списки по умолчанию в «Дереве тем»

### Изменение

**Файл: `src/pages/TopicTree.tsx`, строка 79**

Заменить `useState(true)` на `useState(false)` в компоненте `TopicNode` — все узлы будут свёрнуты при загрузке страницы.


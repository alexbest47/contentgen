

## Добавить кнопку «Скачать» в диалог экспорта HTML

### Изменение

**`src/pages/EmailBuilder.tsx`** (строки 853-859)

Добавить кнопку «Скачать» рядом с «Скопировать». По клику — создать Blob из HTML, скачать как `.html` файл с именем из `title`.

```text
Диалог «Экспорт HTML»:
  [textarea с HTML]
  [Скопировать]  [Скачать]   ← новая кнопка
```

Импортировать `Download` из lucide-react. Кнопки обернуть в `flex gap-2`.




## Удалить кнопки генерации со страницы списка офферов

На странице `OfferTypeDetail.tsx` (список офферов внутри типа) в каждой строке оффера есть две кнопки — «Сгенерировать лид-магниты» (Sparkles) и «Сгенерировать справочный материал» (СМ). Эти кнопки здесь не нужны: генерация контента происходит на следующем уровне, внутри конкретного оффера.

### Изменения

**`src/pages/OfferTypeDetail.tsx`** — удалить строки 493-508 (две кнопки генерации `Sparkles` и `СМ`) из каждой строки оффера.

Также можно удалить связанный код, который больше не используется на этой странице:
- `generateMutation` и `generatingOfferId` state (если они не используются нигде больше на странице)
- Импорт `Sparkles` (если больше не используется)
- Функция `generateProjectAndContent` и связанные state (`progressText`, `setProgressText`)

Итого: в строке оффера останутся только дата, кнопка редактирования, кнопка удаления и стрелка навигации.


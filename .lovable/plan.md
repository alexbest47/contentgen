

## Показать теги программ в списке

### Изменения

**`src/pages/Programs.tsx`**

1. Добавить запрос `program_tags` с джойном на `tags(name)` для получения маппинга `program_id → tag_name[]`
2. В каждой строке программы между описанием и датой показать теги как `Badge` (variant="secondary", маленькие)
3. Импортировать `Badge` из `@/components/ui/badge`

### Итого
- 1 файл, ~15 строк добавлено

